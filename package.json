{
  "name": "background-job-service-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo orchestrating Background Job Service backend and Next.js frontend",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"npm run --prefix backend start:dev\" \"npm run --prefix frontend dev\"",
    "build": "npm run --prefix backend build && npm run --prefix frontend build",
    "test": "npm run --prefix backend test",
    "test:e2e": "npm run --prefix backend test:e2e",
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}